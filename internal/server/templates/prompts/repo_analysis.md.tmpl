# Repository analysis

Job: {{.JobID}}
Subject: {{.SubjectType}} {{.SubjectID}}

You are analysing a single repository as part of a repo-by-repo modularisation workflow.

Inspect the repository at {{.WorkspaceReadRoot}}/repo and propose reusable candidates.

Do not analyse unrelated repositories unless explicitly instructed.
Do not modify code during discovery.
Return candidates for this repository only.

{{if .SpecificFunctionality}}
## User intent

Prioritise reusable candidates that support this requested functionality:
{{.SpecificFunctionality}}

{{if .AreasOfInterest}}Areas of interest:
{{range .AreasOfInterest}}- {{.}}
{{end}}{{end}}{{if .SourceHints}}Source hints:
{{range .SourceHints}}- {{.}}
{{end}}{{end}}{{if .AvoidPatterns}}Avoid patterns:
{{range .AvoidPatterns}}- {{.}}
{{end}}{{end}}{{if .PreferredTargetLanguage}}Preferred target language: {{.PreferredTargetLanguage}}
{{end}}Allow additional agent discovery: {{.AllowAgentDiscovery}}
{{else}}
The user did not provide a narrower extraction target. Inspect the repository and propose reusable candidates yourself.
{{end}}

## Candidate requirements

Each candidate must describe:
- candidate ID, using the session namespace pattern {{.SubjectID}}.cand.NNN in reasoning even though the backend assigns persisted IDs
- repository/session scope
- source paths
- functionality description
- why it is reusable
- project-specific coupling
- suggested module kind
- target language
- dependencies
- risks
- tests found
- missing tests
- extraction notes
- proposed typed ports
- proposed workbench node shape

Focus especially on:
- OpenTelemetry metrics
- traces and logs
- agent lifecycle events
- agent administration
- orchestration
- process supervision
- workspace management
- repository checkout
- task queues
- model/provider abstraction
- deployment helpers
- webhooks
- audit logs
- admin APIs

## Output contract

{{.OutputContract}}

Emit only the CandidateReport JSON object. Do not wrap it in Markdown or prose.
The backend captures stdout and writes {{.CandidateReportPath}} if needed.
Write any supporting logs/artifacts under {{.OutputRoot}} only if they are necessary.

{{if .StructuredOutputSchema}}
Before emitting the final JSON, validate a draft with outvalid:

```bash
{{.OutvalidPath}} --schema {{.StructuredOutputSchemaPath}} --input /tmp/code-workbench-candidate-report-draft.json --writeTo {{.CandidateReportPath}}
```

If validation fails, read the numbered errors, fix the draft, and retry. Allow up to 3 validation attempts.
After validation passes, emit the same validated CandidateReport JSON on stdout.

The JSON object must match this JSON Schema:

```json
{{.StructuredOutputSchema}}
```
{{end}}

## Safety rules

{{.DeniedPathRules}}
Read only from {{.WorkspaceReadRoot}}. Write only under {{.OutputRoot}} and {{.CandidateReportPath}}.
